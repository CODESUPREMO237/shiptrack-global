import AdminLoginCard from "@/components/AdminLoginCard";

export default function AdminLogin() {
  return (
    <AdminLoginCard
      title="Admin Login"
      subtitle="Use your Supabase admin credentials to manage shipments, messages, and feedback."
    />
  );
}
